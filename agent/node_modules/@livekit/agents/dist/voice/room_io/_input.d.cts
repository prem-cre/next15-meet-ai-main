import { type AudioFrame, FrameProcessor, type NoiseCancellationOptions, RemoteParticipant, type Room } from '@livekit/rtc-node';
import { AudioInput } from '../io.js';
export declare class ParticipantAudioInputStream extends AudioInput {
    private room;
    private sampleRate;
    private numChannels;
    private noiseCancellation?;
    private frameProcessor?;
    private publication;
    private participantIdentity;
    private currentInputId;
    private logger;
    constructor({ room, sampleRate, numChannels, noiseCancellation, }: {
        room: Room;
        sampleRate: number;
        numChannels: number;
        noiseCancellation?: NoiseCancellationOptions | FrameProcessor<AudioFrame>;
    });
    setParticipant(participant: RemoteParticipant | string | null): void;
    private onTrackUnpublished;
    private closeStream;
    private onTrackSubscribed;
    private onTokenRefreshed;
    private createStream;
    close(): Promise<void>;
}
//# sourceMappingURL=_input.d.ts.map